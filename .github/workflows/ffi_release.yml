name: Release FFI Library

on:
  release:
    types: [published]

jobs:
  build:
    name: Cross-compile Rust FFI Library
    runs-on: ubuntu-latest

    strategy:
      matrix:
        target:
          - x86_64-unknown-linux-gnu
          # - x86_64-pc-windows-gnu
          # - x86_64-apple-darwin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
          target: ${{ matrix.target }}

      - name: install dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Install dependencies for cross-compilation (Windows)
        if: matrix.target == 'x86_64-pc-windows-gnu'
        run: sudo apt-get update && sudo apt-get install -y gcc-mingw-w64

      - name: Build library
        working-directory: ffi
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package artifacts
        run: |
          mkdir -p artifacts
          cp ffi/target/${{ matrix.target }}/release/lib* artifacts/ || echo "No output libraries found"

      - name: Upload release artifacts
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          name: "Artifacts for ${{ matrix.target }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
